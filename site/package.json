{
  "name": "klarow-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/straznik.mjs",
    "preview": "astro preview",
    "check": "astro check --minimumFailingSeverity error",
    "test": "node --test src/lib/*.test.mjs"
  },
  "dependencies": {
    "astro": "^7.3.3",
    "@fontsource-variable/bricolage-grotesque": "^5.2.5",
    "@fontsource/jetbrains-mono": "^5.2.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "~6.0"
  }
}
